# 感想
题目不难，逻辑甚至是重复的，但是，就是有一个地方，错误是出现过的，但是换了个数据类型，又错了，并且花费了很长时间也没查出错

## 复习盘点
在对栈顶作任何操作之前，应检查栈是否为空

## 思想过程
（第一次）很简单，就是对一段字符串，若遇到左括号就入栈，遇到右括号就检查是否匹配，若匹配则出栈，最后判断栈是否为空，为空说明对了  
（第二次）（改错后）为空有可能是压根没遇到左括号，一直给我右括号，此时匹配条件要严格，让他标记错误然后break出来  
错误：  
1. 入过栈，不为空  
2. 没入过栈，为空（给我一堆右括号）  

正确：  

1. 入过栈，为空
2. 没入过栈，为空（让我判断0个括号匹配）

故而不为空一定错，括号不匹配因而不入栈也错，就这两点  
## 错误罗列
1. 没有判断栈是否为空就访问top元素 <==>没判断p是否为空，就访问其data -->习惯性错误  
   推广到-->访问什么东西都是，他存在吗，他的数据存在吗？ 
2. 输入考虑的还不够，都没想到他能不给我左括号 --> 算是边界考虑不全，测试用例不完整，那么测试用例需要极端输入：长输入，空，单一输入，错误输入（目前题目一般不要求这个，但考虑是对的），进行混合操作的输入（如入队出队）  

## 具体实现
 ```cpp
   string str;
cin >> str;
int len = str.length();
stack<char> s;
int isTrue = 1;
for (int i = 0; i < len; i++)
{
	char e = str[i];
	//左括号入栈
	if (e == '(' || e == '[' || e == '{')
	{
		s.push(e);
		continue;
	}
	//右括号做匹配
		if (e == ')')
		{
			//匹配成功出栈
			if (!s.empty()&&s.top() == '(')
			{
				s.pop();
			}
			//匹配失败就错误了，需要结束循环
			else 
			{
				isTrue = 0;
				break;
			}
		}
		if (e == ']')
		{
			if (!s.empty() && s.top() == '[')
			{
				s.pop();
			}
			else 
			{
				isTrue = 0;
				break;
			}
		}
		if (e == '}')
		{
			if (!s.empty() && s.top() == '{')
			{
				s.pop();
			}
			else 
			{
				isTrue = 0;
				break;
			}
		}
	
}

//两个条件都满足才正确
if (s.empty()&&isTrue)
{
	cout << "ok" << endl;
}
else cout << "error" << endl;
```
