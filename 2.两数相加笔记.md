
## 要解决的问题
1.产生进位，如何加到后一个数据-循环，这次的进位下一次就能用上  
2.没想到的点：最后一位要是有进位应该创建新结点  

## 解决过程
先想边界情况：0+0和最后一位有进位的  
画图表示过程：发现若一个链表结束了要处理的情况分两种  
1.若无进位则为另一个链表的元素  
2.若有进位则也相加  

## 出现的问题
1. 超时--迭代器没迭代
2. 输入奇怪不知道怎么输入--不管，改造成普通输入，因为这部分归力扣管
3. 无法写入问题--有野指针，检查指针，if(!q)这样的错误，应多想一下if怎样进
4. 有些情况少位--最后一位进位要加新结点
5. 输出和vs不同（第一位不做加法）--题目用的是没有头结点的链表
6. 加新结点无法与前面关联==因为迭代器失效了，无法找到前一位，正确答案是要新建结点而不是使用现有结点，就都解决了

## 复习简要
1. if判断多想一下进的条件
2. 对于牺牲一点空间换取减少指针操作或时间，可以接受
3. 链表很灵活，必要结构只有结点与连接，头结点头指针尾指针都可以没有（最好多一点指针，方便操作）
4. 没有头结点的操作：创建一个虚拟头结点，就是它本身不在我链表里，但我还是当头结点在用


```cpp
//我的解法
LinkList& LinkListadd(LinkList& l1, LinkList& l2)
{
	//在原有链表上操作，相对麻烦

	//所有判断都判断下一位是否为空，以使结束后迭代器仍有效以添加最后进位的新结点
	Lnode* p = l1->next;
	Lnode* q = l2->next;

	int count = 0;
	while (p && q)
	{
		p->data += q->data + count;
		if (p->data >= 10)
		{
			p->data -= 10;
			count = 1;
		}
		else count = 0;
		p = p->next;
		q = q->next;
	}
	while (p)
	{
		p->data += count;
		if (p->data >= 10)
		{
			p->data -= 10;
			count = 1;
		}
		else count = 0;
		p = p->next;
	}
	while (q)
	{
		p->next = q;
		q->data += count;
		if (q->data >= 10)
		{
			q->data -= 10;
			count = 1;
		}
		else count = 0;
		q = q->next;
		p = p->next;
	}
	if (count == 1)
	{
		Lnode* lastone = new Lnode;
		lastone->data = 1;
		lastone->next = NULL;
		p = lastone;
	}
	return l1;
}
```

---
```cpp
//答案方法
LinkList LinkListadd_better(LinkList& l1, LinkList& l2)
{
	Lnode* p = l1;
	Lnode* q = l2;

	//创建新链表
	Lnode* head = NULL,* tail = NULL;
	int count = 0;
	while (p || q)
	{
		//将不够长的链表补0
		int n1 = p ? p->data:0;
		int n2 = q ?  q->data:0;

		int sum = n1 + n2 + count;
		count = sum / 10;
		
		//若头指针为空，说明还没有元素
		if (!head)
		{
			head = tail = new Lnode(sum % 10);
		}
		else
		//依次往后加结点即可
		{
			tail->next = new Lnode(sum % 10);
			tail = tail->next;
		}
		if (p)
		{
			p = p->next;
		}
		if (q)
		{
			q = q->next;
		}
	}

	//处理最后有进位情况
	if (count > 0)
	{
		tail->next = new Lnode(count);
	}
	return head;
}
```

